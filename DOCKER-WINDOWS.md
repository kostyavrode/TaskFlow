# 🐳 TaskFlow на Docker Desktop (Windows)

Простое руководство по запуску TaskFlow через Docker Desktop на Windows.

## 📋 Требования

1. **Docker Desktop для Windows** - [Скачать](https://www.docker.com/products/docker-desktop/)
2. Docker Desktop должен быть запущен

## 🚀 Быстрый старт

### Вариант 1: Использовать PowerShell скрипт (Рекомендуется)

1. **Откройте PowerShell** в папке проекта

2. **Запустите скрипт**:
   ```powershell
   .\scripts\docker-start.ps1
   ```

3. **Дождитесь запуска** (первый раз может занять 5-10 минут на сборку образов)

4. **Откройте Web UI**:
   - Откройте файл `web/index.html` в браузере
   - Или используйте Swagger: http://localhost:5000/swagger

### Вариант 2: Ручной запуск

1. **Откройте PowerShell** в папке `infrastructure`:
   ```powershell
   cd infrastructure
   ```

2. **Запустите сервисы**:
   ```powershell
   docker-compose up -d --build
   ```

3. **Проверьте статус**:
   ```powershell
   docker-compose ps
   ```

## 📊 Доступ к сервисам

После запуска доступны:

- **Task Management API**: http://localhost:5000
- **Swagger UI**: http://localhost:5000/swagger
- **Notification API**: http://localhost:5002
- **RabbitMQ Management**: http://localhost:15672
  - Логин: `guest`
  - Пароль: `guest`

## 🛠️ Полезные команды

### Остановить все сервисы
```powershell
.\scripts\docker-stop.ps1
```

### Просмотр логов
```powershell
# Все сервисы
.\scripts\docker-logs.ps1

# Конкретный сервис
.\scripts\docker-logs.ps1 -Service task-management-api
```

### Перезапустить сервис
```powershell
cd infrastructure
docker-compose restart task-management-api
```

### Просмотр статуса
```powershell
cd infrastructure
docker-compose ps
```

### Просмотр использования ресурсов
```powershell
docker stats
```

## 🔧 Устранение проблем

### Docker Desktop не запускается

1. Убедитесь, что WSL 2 установлен
2. Перезагрузите компьютер
3. Проверьте, что виртуализация включена в BIOS

### Порт уже занят

Если порт 5000, 5002 или 15672 занят:

1. Остановите сервисы, использующие эти порты
2. Или измените порты в `infrastructure/docker-compose.yml`:
   ```yaml
   ports:
     - "5001:8080"  # Вместо 5000:8080
   ```

### Сервисы не запускаются

1. **Проверьте логи**:
   ```powershell
   cd infrastructure
   docker-compose logs
   ```

2. **Проверьте место на диске**:
   ```powershell
   docker system df
   ```

3. **Очистите неиспользуемые ресурсы**:
   ```powershell
   docker system prune -a
   ```

### Ошибка "Cannot connect to Docker daemon"

1. Убедитесь, что Docker Desktop запущен
2. Подождите, пока Docker Desktop полностью загрузится
3. Попробуйте перезапустить Docker Desktop

## 🗑️ Очистка

### Удалить все контейнеры и сети
```powershell
cd infrastructure
docker-compose down
```

### Удалить все (включая volumes и образы)
```powershell
cd infrastructure
docker-compose down -v --rmi all
```

## 📝 Структура сервисов

- **task-management-api** - API для управления задачами
- **task-execution-worker** - Фоновый сервис для выполнения задач
- **notification-api** - API для уведомлений (SignalR)
- **postgres-task-management** - База данных для Task Management
- **postgres-task-execution** - База данных для Task Execution
- **rabbitmq** - Очередь сообщений

## 💡 Советы

1. **Первый запуск** может занять время на скачивание образов
2. **Последующие запуски** будут быстрее благодаря кэшу
3. **Docker Desktop** должен быть запущен перед запуском скрипта
4. Используйте **Docker Desktop UI** для мониторинга контейнеров

## 🔗 Дополнительная информация

- Подробное руководство: [infrastructure/DEPLOYMENT.md](infrastructure/DEPLOYMENT.md)
- Общая документация по Docker: [DOCKER.md](DOCKER.md)

